# Required — backend API URL (no trailing slash)
VITE_API_BASE_URL=http://localhost:8000

# Optional — reduces Google Books API rate-limit issues for AI suggestion covers
# VITE_GOOGLE_BOOKS_API_KEY=
